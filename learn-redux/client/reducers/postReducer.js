//reducer takes in 2 things, 1 the action and 2 the state(Store)
////then return a copy of the updated state(sotre)
////

function postReducer(state = [], action) {
  console.log(state, action)
return state
}

export default postReducer
